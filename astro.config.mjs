import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // Para que funcione en GitHub Pages
    site: 'https://Sebas5158R.github.io',
    base: 'solar-system-astro',
});
